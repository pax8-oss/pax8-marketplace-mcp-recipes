default: true
MD013: false  # line-length — too strict for docs with URLs and tables
MD034: false  # no-bare-urls — email addresses in prose are intentional
MD041: false  # first-line-heading — PR templates legitimately start with ##
